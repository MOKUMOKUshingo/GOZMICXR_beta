# ADD 33 - XRRightHand.glb hand model replacement

Base: ADD32 AR spherical/scrub hand-default version.

Changes:
- Replaced previous XR hand model with `three_scene/assets/XRRightHand.glb`.
- Right hand uses XRRightHand.glb directly.
- Left hand is generated by mirroring the right-hand GLB on local X.
- Grip animation uses `Grip` if present, otherwise `Take 001`, otherwise the first animation clip.
- Other Add32 settings are preserved: AR spherical screen controls, VR/AR scrub bar, video settings, VR movement, and scene layout.

`video/projectmovie1.mp4` is still not bundled. Put it in the `video/` folder when testing.
